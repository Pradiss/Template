<section id="depoimentos" class="relative overflow-hidden bg-zinc-900 py-24 sm:py-32">
  <div class="absolute inset-0 bg-[radial-gradient(circle_at_top,_rgba(255,255,255,0.08),_transparent_35%)]"></div>

  <div class="relative mx-auto max-w-7xl px-6 lg:px-8">
    <div class="mx-auto max-w-3xl text-center">
      <p class="text-xs font-bold uppercase tracking-[0.25em] text-zinc-400">
        Depoimentos
      </p>

      <h2 class="mt-6 text-4xl font-semibold tracking-tight text-white sm:text-6xl">
        O que nossos clientes dizem
      </h2>
    </div>
  </div>

  <div class="relative mt-20">
    <div class="pointer-events-none absolute inset-y-0 left-0 z-10 hidden w-24 bg-gradient-to-r from-zinc-900 to-transparent sm:block"></div>
    <div class="pointer-events-none absolute inset-y-0 right-0 z-10 hidden w-24 bg-gradient-to-l from-zinc-900 to-transparent sm:block"></div>

    <div
      id="testimonials-carousel"
      class="no-scrollbar flex cursor-grab snap-x snap-mandatory gap-6 overflow-x-auto scroll-smooth px-6 active:cursor-grabbing lg:px-[calc((100vw-80rem)/2+1.5rem)]"
    >
      <article class="flex min-h-[340px] w-[85vw] shrink-0 snap-center flex-col justify-between rounded-2xl border border-white/10 bg-white/[0.03] p-8 shadow-2xl shadow-black/10 sm:w-[460px]">
        <p class="text-xl leading-relaxed tracking-tight text-zinc-100 sm:text-2xl">
          “Percebemos uma evolução clara na apresentação da nossa empresa. O site ficou mais moderno, objetivo e fácil de navegar.”
        </p>

        <div class="mt-10">
          <p class="text-sm font-semibold text-white">Mariana Lopes</p>
          <p class="mt-1 text-sm text-zinc-400">Diretora comercial</p>
        </div>
      </article>

      <article class="flex min-h-[340px] w-[85vw] shrink-0 snap-center flex-col justify-between rounded-2xl border border-white/10 bg-white/[0.03] p-8 shadow-2xl shadow-black/10 sm:w-[460px]">
        <p class="text-xl leading-relaxed tracking-tight text-zinc-100 sm:text-2xl">
          “A landing page ajudou muito nas campanhas. Ficou rápida, bonita e com as informações organizadas do jeito que precisávamos.”
        </p>

        <div class="mt-10">
          <p class="text-sm font-semibold text-white">Renato Martins</p>
          <p class="mt-1 text-sm text-zinc-400">Gestor de marketing</p>
        </div>
      </article>

      <article class="flex min-h-[340px] w-[85vw] shrink-0 snap-center flex-col justify-between rounded-2xl border border-white/10 bg-white/[0.03] p-8 shadow-2xl shadow-black/10 sm:w-[460px]">
        <p class="text-xl leading-relaxed tracking-tight text-zinc-100 sm:text-2xl">
          “O processo foi simples e o resultado passou muito mais confiança para nossos clientes. Agora temos uma presença digital mais profissional.”
        </p>

        <div class="mt-10">
          <p class="text-sm font-semibold text-white">Camila Andrade</p>
          <p class="mt-1 text-sm text-zinc-400">Empresária</p>
        </div>
      </article>

      <article class="flex min-h-[340px] w-[85vw] shrink-0 snap-center flex-col justify-between rounded-2xl border border-white/10 bg-white/[0.03] p-8 shadow-2xl shadow-black/10 sm:w-[460px]">
        <p class="text-xl leading-relaxed tracking-tight text-zinc-100 sm:text-2xl">
          “Ficou muito mais fácil explicar nossos serviços. O novo site deixou a comunicação clara e trouxe mais segurança para o cliente.”
        </p>

        <div class="mt-10">
          <p class="text-sm font-semibold text-white">André Silva</p>
          <p class="mt-1 text-sm text-zinc-400">Sócio fundador</p>
        </div>
      </article>

      <article class="flex min-h-[340px] w-[85vw] shrink-0 snap-center flex-col justify-between rounded-2xl border border-white/10 bg-white/[0.03] p-8 shadow-2xl shadow-black/10 sm:w-[460px]">
        <p class="text-xl leading-relaxed tracking-tight text-zinc-100 sm:text-2xl">
          “O visual ficou elegante e profissional. A página passou a representar melhor o posicionamento da nossa marca.”
        </p>

        <div class="mt-10">
          <p class="text-sm font-semibold text-white">Juliana Costa</p>
          <p class="mt-1 text-sm text-zinc-400">CEO</p>
        </div>
      </article>

      <article class="flex min-h-[340px] w-[85vw] shrink-0 snap-center flex-col justify-between rounded-2xl border border-white/10 bg-white/[0.03] p-8 shadow-2xl shadow-black/10 sm:w-[460px]">
        <p class="text-xl leading-relaxed tracking-tight text-zinc-100 sm:text-2xl">
          “A experiência foi muito boa. Recebemos uma estrutura rápida, bonita e pronta para campanhas de tráfego.”
        </p>

        <div class="mt-10">
          <p class="text-sm font-semibold text-white">Felipe Rocha</p>
          <p class="mt-1 text-sm text-zinc-400">Coordenador de vendas</p>
        </div>
      </article>
    </div>
  </div>

  <div class="relative mx-auto mt-10 flex max-w-7xl items-center justify-center gap-3 px-6 lg:px-8">
    <button
      type="button"
      data-carousel-prev
      aria-label="Depoimento anterior"
      class="flex h-11 w-11 items-center justify-center rounded-full border border-white/10 bg-white/10 text-white transition hover:bg-white hover:text-zinc-950"
    >
      ←
    </button>

    <button
      type="button"
      data-carousel-next
      aria-label="Próximo depoimento"
      class="flex h-11 w-11 items-center justify-center rounded-full border border-white/10 bg-white/10 text-white transition hover:bg-white hover:text-zinc-950"
    >
      →
    </button>
  </div>

  <div class="relative mx-auto max-w-7xl px-6 lg:px-8">
    <p class="mx-auto mt-12 max-w-4xl text-center text-sm leading-6 text-zinc-500">
      Os depoimentos representam experiências individuais de clientes e podem variar conforme o projeto, segmento e objetivos de cada empresa.
    </p>
  </div>
</section>

<style>
  .no-scrollbar {
    
    -ms-overflow-style: none;
    scrollbar-width: none;
  }

  .no-scrollbar::-webkit-scrollbar {
    display: none;
  }
</style>

<script>
  const carousel = document.querySelector("#testimonials-carousel");
  const prevButton = document.querySelector("[data-carousel-prev]");
  const nextButton = document.querySelector("[data-carousel-next]");

  if (carousel instanceof HTMLElement) {
    let isDown = false;
    let startX = 0;
    let scrollLeft = 0;

    carousel.addEventListener("mousedown", (event) => {
      isDown = true;
      carousel.classList.add("select-none");
      startX = event.pageX - carousel.offsetLeft;
      scrollLeft = carousel.scrollLeft;
    });

    carousel.addEventListener("mouseleave", () => {
      isDown = false;
      carousel.classList.remove("select-none");
    });

    carousel.addEventListener("mouseup", () => {
      isDown = false;
      carousel.classList.remove("select-none");
    });

    carousel.addEventListener("mousemove", (event) => {
      if (!isDown) return;

      event.preventDefault();

      const x = event.pageX - carousel.offsetLeft;
      const walk = (x - startX) * 1.4;

      carousel.scrollLeft = scrollLeft - walk;
    });

    const scrollAmount = () => {
      const firstCard = carousel.querySelector("article");

      if (firstCard instanceof HTMLElement) {
        return firstCard.offsetWidth + 24;
      }

      return 460;
    };

    prevButton?.addEventListener("click", () => {
      carousel.scrollBy({
        left: -scrollAmount(),
        behavior: "smooth",
      });
    });

    nextButton?.addEventListener("click", () => {
      carousel.scrollBy({
        left: scrollAmount(),
        behavior: "smooth",
      });
    });
  }
</script>